# need two windows
# one for the index and type select
# one for the camera output
# v1
# the selector needs to show a text box for index and type

# the camera output needs to show the same as the previous editor
# and call the same get angle, get position functions as the tracker

